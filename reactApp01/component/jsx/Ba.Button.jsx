import React from 'react';

class ClockButton extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <button>计数</button>
        );
    }
}
export default ClockButton;
